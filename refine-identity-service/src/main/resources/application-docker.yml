spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848
  data:
    redis:
      host: identity-redis
  rabbitmq:
    host: rabbitmq

refine:
  datasource:
    primary-url: jdbc:mysql://mysql-primary:3306/identity_db?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    replica-url: jdbc:mysql://mysql-replica:3306/identity_db?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
