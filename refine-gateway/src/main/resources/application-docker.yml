spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848
    sentinel:
      transport:
        dashboard: sentinel:8858
      datasource:
        gateway-rules:
          nacos:
            server-addr: nacos:8848
